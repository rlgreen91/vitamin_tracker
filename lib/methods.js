// Meteor.methods({
// 	getFoodno: function (foodname) {
// 		//Get the food NDB number based on the food name

// 		var api_url = "http://api.nal.usda.gov/ndb/search?q=" + foodname + "&api_key=DEMO_KEY";

// 		var api_response = HTTP.call("GET", api_url, asyncCallback());

// 		return api_response;
// 	}
// })